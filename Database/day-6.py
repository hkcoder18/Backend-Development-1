# JOIN

# Join is used to combine rows from 2 or more tables.

# INNER JOIN - Returns only matching(common) rows
# LEFT JOIN - All rows in left table if not matching it gives null.
# RIGHT JOIN - All rows in right table if not matching it gives null.
# FULL JOIN - All data in both tables if not matching it gives null.